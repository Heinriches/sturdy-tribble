/**
 * ELSE STATEMENT
 * You can use the 'else' statement after an 'if' statement
 * if you want to run a code in case the condition is not satisfied.
 * 
 * For example:
 */


#include <stdio.h>
int main()
{
  int score = 87;
  
  if(score >= 100){
    printf("Level Completed!");
  }
  else{
    printf("Game Over");
  }
  
  return 0;
}

/**
 * Open and run the code to see how it works, based on the value
 * of the variable.
 */