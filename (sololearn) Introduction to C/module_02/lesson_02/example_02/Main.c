/**
 * IF STATEMENT
 * Remember that you need to use two equal equal signs '=='
 * to test for equality, since a single equal sign is 
 * the assignment operator.
 * 
 * For example, let's check the position value for equality:
 */


#include <stdio.h>
int main()
{
  int position = 2;
  
  if(position ==2){
    printf("Silver");
  }
  
  return 0;
}